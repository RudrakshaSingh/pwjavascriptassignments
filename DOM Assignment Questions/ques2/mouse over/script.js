const box = document.getElementById("box");

// add a mouseover event listener to the element
box.addEventListener("mouseover", function () {
  alert(
    "mouse over event is working properly"
  );
});